spring:
  application:
    name: BLOG-CAPTCHA-SERVICE
  cloud:
    nacos:
      server-addr: 10.0.4.16:8848
      username: nacos
      password: nacos
      config:
        name: ${spring.application.name}
        file-extension: YAML
        group: BLOG