spring:
  application:
    name: BLOG-CAPTCHA-SERVICE
  cloud:
    nacos:
      server-addr: 43.143.70.32:8848
      config:
        name: ${spring.application.name}
        file-extension: YAML
        group: BLOG