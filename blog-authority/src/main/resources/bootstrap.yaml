spring:
  application:
    name: BLOG-AUTHORITY-SERVICE
  cloud:
    nacos:
      config:
        shared-configs:
          - data-id: COMMON.YAML
            group: BLOG
        name: ${spring.application.name}
        file-extension: YAML
        group: BLOG
